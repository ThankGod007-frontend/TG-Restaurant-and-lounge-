// Function to handle "Reserve a Table" button click
function reserveNow() {
  alert("Redirecting to the reservations page...");
  // Redirect to a reservations page (to be created later)
  window.location.href = "#reservations";
}
